# Copy to .env (which is gitignored) before running `docker compose up`.
MYSQL_DATABASE=rxn_app
MYSQL_USER=app_user
MYSQL_PASSWORD=change-me
MYSQL_ROOT_PASSWORD=change-me-too

# Set to 1 to build the PHP image with Xdebug enabled.
INSTALL_XDEBUG=0
